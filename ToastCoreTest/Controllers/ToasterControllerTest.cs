using Microsoft.EntityFrameworkCore;
using Microsoft.Extensions.Configuration;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using System.Linq;
using ToastCore.Controllers;
using ToastCore.Models;

namespace ToastCoreTest
{
    [TestClass]
    public class ToasterControllerTest
    {
        //private ToastCoreContext db = new ToastCoreContext();

        //[TestMethod]
        //public void GetToastersTest()
        //{
        //    var controller = new ToasterController(db);

        //    Assert.IsTrue(controller.GetToasters().Count() > 0, "There must be at least a toaster.");
        //}
    }
}
